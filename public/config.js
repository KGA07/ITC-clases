// Configuracion publica del frontend.
// Editá estos valores luego del deploy con las URLs finales de tus proyectos.
window.ITC_CONFIG = {
  // URL base del proyecto EvaluacionITC (para el boton "Evaluaciones").
  evaluacionesUrl: '/',
  // URL base de este mismo proyecto (para compartir/abrir el sitio).
  clasesUrl: '/'
};